• isalpha DONE
• isdigit DONE
• isalnum DONE
• isascii DONE
• isprint DONE
• strlen  DONE
• memset  
• bzero
• memcpy
• memmove
• strlcpy DONE
• strlcat DONE
• toupper DONE
• tolower DONE
• strchr  DONE
• strrchr DONE
• strncmp DONE
• memchr
• memcmp
• strnstr DONE
• atoi    DONE

void bzero(void * s, size_t n)
{
  char * c = s; // Can't work with void *s directly.
  size_t i;
  for (i = 0; i < n; ++i)
    c[i] = '\0';
}
 implement memset
 bzero will call memset with the char '\0' 
 
 void * memset( void *s, int c, size_t n )
{
    for ( unsigned char *p = s, ch = c; n != 0; n-- )
    {
        *p++ = ch;
    }

    return s;
} ???? will thius actually work? does it fill byte by byte the way memset does?

16843009 
